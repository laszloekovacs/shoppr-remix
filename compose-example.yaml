version: '3.8'
volumes:
  mongo_data:
  shoppr_data:

services:
  shoppr:
    image: ghcr.io/laszloekovacs/shoppr-remix:latest
    container_name: shoppr
    volumes:
      - shoppr_data:/app/public/uploads
    ports:
      - 3000:3000
    depends_on:
      - mongo
    env_file:
      - .env.shoppr
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.shoppr.rule=Host(`hostname.example.com`)'
      - 'traefik.http.routers.shoppr.entrypoints=websecure'
      - 'traefik.http.routers.shoppr.tls.certresolver=myresolver'
      - 'traefik.http.routers.shoppr.service=shoppr'

  # use own mongodb if hw supports it
  mongo:
    image: mongo:latest
    container_name: mongo
    volumes:
      - mongo_data:/data/db
    ports:
      - 27017:27017
    env_file:
      - .env.shoppr
